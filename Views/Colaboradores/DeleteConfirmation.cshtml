@model VigiLant.Models.Colaborador
@{
    ViewData["Title"] = "Confirmar Exclusão";
}

@section Styles {
    <link rel="stylesheet" href="/css/viewrevo.css" asp-append-version="true" />
}

<div class="page-header">
    <h1 class="page-title">Confirmar Exclusão do Colaborador</h1>
    <div class="breadcrumb">
        <a href="@Url.Action("Index", "Home")">Dashboard</a>
        <span class="separator">/</span>
        <a href="@Url.Action("Index")">Colaboradores</a>
        <span class="separator">/</span>
        <span class="active">Excluir</span>
    </div>
</div>

<div class="card p-4 bg-danger-light text-danger border-danger">
    <h3 class="card-title text-danger">⚠️ Atenção: Excluir Colaborador</h3>
    <p>Você tem certeza que deseja **excluir permanentemente** o colaborador:</p>
    <div class="alert alert-warning" role="alert">
        Esta ação é **irreversível** e removerá todos os dados de registro deste colaborador.
    </div>
    
    <dl class="row mt-4">
        <dt class="col-sm-3">ID</dt>
        <dd class="col-sm-9">#@Model.Id</dd>

        <dt class="col-sm-3">Nome</dt>
        <dd class="col-sm-9">**@Model.Nome**</dd>

        <dt class="col-sm-3">Cargo</dt>
        <dd class="col-sm-9">@Model.Cargo</dd>
    </dl>
    
    <form asp-action="Delete" method="post" asp-route-id="@Model.Id" class="mt-4">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn-danger">
            <i class="fas fa-trash-alt"></i> Sim, Excluir Permanentemente
        </button>
        <a href="@Url.Action("Index")" class="btn-secondary">
            <i class="fas fa-times"></i> Cancelar
        </a>
    </form>
</div>