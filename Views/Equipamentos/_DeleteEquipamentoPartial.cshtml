@model VigiLant.Models.Equipamento

<link rel="stylesheet" href="/css/viewrevo.css" asp-append-version="true" />

<div class="delete-message p-3" style="background-color: #f8d7da; border: 1px solid #f5c6cb; border-radius: .25rem;">
    <h4 class="card-title text-danger">⚠️ Confirmação de Desconexão</h4>
    <p class="text-danger">Você tem certeza que deseja <b>desconectar e excluir</b> permanentemente o equipamento:</p>

    <div class="details-container">
        <div class="row">
            <div class="col-sm-6">Nome do Equipamento:</div>
            <div class="col-sm-6"><b>@Model.Nome</b></div>
        </div>
        <hr class="divider" />
        <div class="row">
            <div class="col-sm-6">Identificador (Broker):</div>
            <div class="col-sm-6">@Model.IdentificadorBroker</div>
        </div>
        <div class="row">
            <div class="col-sm-6">ID Interno:</div>
            <div class="col-sm-6">#@Model.Id</div>
        </div>
    </div>

    <div class="alert alert-warning" role="alert" style="margin-top: 15px;">
        Esta ação é <b>irreversível</b> e removerá todos os registros de monitoramento deste equipamento do nosso banco de dados.
    </div>
</div>

<form asp-action="DeleteConfirmed" method="post" id="deleteForm">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="Id" />

    <div class="form-group mt-4 text-end">
        <button type="submit" class="btn-primary">
            <i class="fas fa-trash"></i> Desconectar e Excluir
        </button>
        <button type="button" class="btn-secondary" id="cancelModalButton">
            <i class="fas fa-arrow-left"></i> Cancelar
        </button>
    </div>
</form>